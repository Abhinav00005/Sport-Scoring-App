<style>
    html,
    body {
        height: 100%;
    }

    body {
        margin: 0;
        background: linear-gradient(45deg, #49a09d, #5f2c82);
        font-family: sans-serif;
        font-weight: 100;
    }

    .container {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

    table {
        width: 800px;
        border-collapse: collapse;
        overflow: hidden;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }

    h1 {
        padding: 15px;
        color: #36e4dc;
    }


    th,
    td {
        padding: 15px;
        background-color: rgba(255, 255, 255, 0.2);
        color: #fff;
    }

    th {
        text-align: left;
        background-color: #55608f;
    }

    .player-box {
        position: absolute;
        top: 50%;
        left: 50%;
        width: 400px;
        padding: 40px;
        transform: translate(-50%, -50%);
        background: rgba(0, 0, 0, .5);
        box-sizing: border-box;
        box-shadow: 0 15px 25px rgba(0, 0, 0, .6);
        border-radius: 10px;
    }

    .player-box h1 {
        margin: 0 0 30px;
        padding: 0;
        text-align: center;
    }

    .player-box .form-field {
        position: relative;
    }

    .player-box .form-field input {
        width: 100%;
        padding: 10px 0;
        font-size: 16px;
        color: #fff;
        margin-bottom: 30px;
        border: none;
        border-bottom: 1px solid #fff;
        outline: none;
        background: transparent;
    }

    .player-box .form-field label {
        position: absolute;
        top: 0;
        left: 0;
        margin-bottom: 5px;
        padding: 10px 0;
        font-size: 16px;
        color: #fff;
        pointer-events: none;
        transition: .5s;
    }

    .player-box .form-field input:focus~label,
    .player-box .form-field input:valid~label {
        top: -20px;
        left: 0;
        color: #36e4dc;
        font-size: 12px;
    }

    .submit {
        padding: 10px 20px;
        color: #36e4dc;
        background-color: inherit;
        border-radius: 10px;
        font-size: 16px;
        text-decoration: none;
        text-transform: uppercase;
        overflow: hidden;
        cursor: pointer;
    }

    a {
        padding: 10px 20px;
        color: #36e4dc;
        border-radius: 10px;
        font-size: 16px;
        text-decoration: none;
        overflow: hidden;
        cursor: pointer;
        background: rgba(0, 0, 0, .5);
        box-sizing: border-box;
        box-shadow: 0 15px 25px rgba(0, 0, 0, .6);
    }

    .submit-btn {
        display: flex;
        justify-content: center;
    }
</style>